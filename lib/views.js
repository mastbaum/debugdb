/**
 * Show functions to be exported from the design doc.
 */
/**
exports.fec_test = {
    map: function(doc) {
        if (doc.type == 'fec_test') {
            emit(doc.id, doc);
        }
    }
};
*/
